{
  "name": "@devatlas/api-client",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "openapi-generator-cli generate -g typescript-fetch -i ../../openapi.json -o ./src/generated --additional-properties=typescriptThreePlus=true,withInterfaces=true",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@devatlas/types": "workspace:^"
  }
}
